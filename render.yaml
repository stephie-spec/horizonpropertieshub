services:
  - type: web
    name: horizon-properties-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        value: your-postgresql-url-here
      - key: SECRET_KEY
        generateValue: true